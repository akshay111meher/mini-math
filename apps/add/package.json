{
  "name": "@mini-math/add-basic",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --watch --loader ts-node/esm src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "lint": "eslint . --ext .ts",
    "format": "prettier --write .",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@mini-math/utils": "^0.1.0"
  },
  "devDependencies": {
    "ts-node": "^10.9.2",
    "@types/node": "^22.7.5"
  }
}
